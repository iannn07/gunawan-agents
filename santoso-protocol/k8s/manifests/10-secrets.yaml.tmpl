# Template — do NOT commit the rendered version.
# deploy-all.sh renders this to 10-secrets.generated.yaml using values from
# santoso-protocol/.env, then kubectl applies it and removes the rendered file.
apiVersion: v1
kind: Secret
metadata:
  name: gunawan-secrets
  namespace: gunawan
type: Opaque
stringData:
  ANTHROPIC_API_KEY: "__ANTHROPIC_API_KEY__"
  TELEGRAM_BOT_TOKEN: "__TELEGRAM_BOT_TOKEN__"
  TELEGRAM_CHAT_ID: "__TELEGRAM_CHAT_ID__"
