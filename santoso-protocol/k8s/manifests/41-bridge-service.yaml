apiVersion: v1
kind: Service
metadata:
  name: telegram-bridge
  namespace: gunawan
  labels:
    app: telegram-bridge
spec:
  type: ClusterIP
  selector:
    app: telegram-bridge
  ports:
    - name: http
      port: 3200
      targetPort: 3200
