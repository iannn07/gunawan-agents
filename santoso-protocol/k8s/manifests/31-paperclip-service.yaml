apiVersion: v1
kind: Service
metadata:
  name: paperclip
  namespace: gunawan
  labels:
    app: paperclip
spec:
  type: ClusterIP
  selector:
    app: paperclip
  ports:
    - name: http
      port: 3100
      targetPort: 3100
